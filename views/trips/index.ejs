<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>Lista podróży</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Twoje podróże</h1>

    <a href="/trips/new" class="btn btn-white">➕ Dodaj nową podróż</a>

    <ul>
      <% trips.forEach(trip => { %>
        <li class="trip-card">
          <h2><%= trip.name %></h2>
          <p><strong>Data:</strong> <%= trip.date %></p>
          <p><strong>Hotel:</strong> <%= trip.hotel %></p>
          <p><strong>Transport:</strong> <%= trip.transport %></p>
          <p><strong>Miejsca:</strong> <%= trip.places %></p>

          <a href="/trips/<%= trip.id %>/edit" class="btn">✏️ Edytuj</a>

          <!-- Кнопка USUŃ -->
          <form action="/trips/<%= trip.id %>/delete" method="POST" style="display:inline;" onsubmit="return confirm('Czy na pewno chcesz usunąć tę podróż?');">
            <button type="submit" class="btn">🗑 Usuń</button>
          </form>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
